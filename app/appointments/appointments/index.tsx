import { View, Text } from 'react-native'
import AppointmentsList from '@/components/appointments/appointments/appointmentsList'

const index = () => {
  return (
    <View>
      <AppointmentsList/>
    </View>
  )
}

export default index